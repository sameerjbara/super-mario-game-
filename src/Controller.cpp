#include "Controller.h"
/*
Controller::Controller()
	:m_window(sf::RenderWindow(sf::VideoMode(1500, 800), "SFML Animation")),gameClock(sf::Clock())
{



}

void Controller::move(sf::Time d)
{

	m_board->m_monster->update(d);
	m_player->move(d);//

}

void Controller::run()
{
	while (m_window.isOpen())
	{
		m_window.clear(m_color);
		m_board.draw(m_window);
		m_window.display();



}
*/